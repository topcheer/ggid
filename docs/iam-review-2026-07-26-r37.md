# IAM 系统深度审查报告 — 2026-07-26 (R37)

## 审查范围

全新视角。聚焦：审计服务多租户隔离、密码重置 token 重放、email 验证 token 重放、新 commit 审查（c_hash/at_hash/brute-force/scope enforcement）。

---

## 1. RFC 标准符合性

### 新增确认（新 commit 修复）

| RFC | 功能 | 状态 | commit |
|-----|------|------|--------|
| OIDC Core §3.1.3.6 | c_hash + at_hash in id_token | ✅ 新修复 | 94362b0bb, 8e58ab895 |
| OIDC Core §12 | id_token on refresh when openid scope | ✅ 新修复 | 7a5d42f41 |
| OIDC §5.4 | userinfo scope-based claims | ✅ 新修复 | cb86d805f |
| OAuth 2.0 | PasswordGrant brute-force (locked_until) | ✅ 新修复 | 368899941 |
| SQL injection | SET app.tenant_id 参数化 | ✅ 新修复 | de0870ca5 |

### 之前报告差距仍存在

- response_mode/prompt/max_age/login_hint 未实现 (P2-31~34)
- OIDC discovery 缺 8 个字段 (P2-1~8)
- JWKS 不暴露旧 key (P1-6)
- webfinger 缺失 (P2-8)

## 2. 竞品迁移成本

之前 R36 报告已覆盖。无新变化。

## 3. 安全审查

### 3.1 审计服务多租户隔离 — ✅ 安全

- `http.go:882`: `WHERE tenant_id = $1` 参数化查询
- `missing_handlers.go:44`: 同上
- webhook 查询: tenant_id 过滤 ✅

### 3.2 密码重置 token 重放 — ✅ 安全

- `password_service.go:162`: `ConsumeResetToken` 使用 `rdb.Del(ctx, key)` — 一次性使用
- token 有 30 分钟 TTL ✅
- hashToken 防止明文存储 ✅

### 3.3 Email 验证 token 重放 — ✅ 安全

- `email_lockout.go:46`: `VerifyEmailToken` 使用 `rdb.Del(ctx, key)` — 一次性使用
- token 有 24 小时 TTL ✅
- GenerateRandomToken(32) 防止猜测 ✅

### 3.4 PasswordGrant 暴力破解 — ✅ 新修复

- `oauth_service.go:1979-2002`: 检查 `locked_until` + 递增 `failed_attempts`
- 超过阈值自动锁定 ✅

### 3.5 SQL injection — ✅ 新修复

- `de0870ca5`: `SET app.tenant_id` 改为参数化（之前用 `fmt.Sprintf`）

## 4. 代码质量 — 最近 10 commits

| Commit | 类型 | 质量 |
|--------|------|------|
| 18ca6218a test | refresh token rotation E2E | ✅ 好 |
| 867b2a362 test | c_hash + auth_time regression | ✅ 好 |
| 94362b0bb fix | id_token c_hash + auth_time | ✅ 正确 |
| cb86d805f fix | userinfo scope enforcement | ✅ 正确 |
| 1740ee898 docs | — | — |
| 7a5d42f41 fix | id_token on refresh | ✅ 正确 |
| 8e58ab895 fix | id_token AMR/ACR + at_hash | ✅ 正确 |
| de0870ca5 security | SQL injection 修复 | ✅ 重要修复 |
| 6706541dc test | PasswordGrant brute-force test | ✅ 好 |
| 368899941 fix | PasswordGrant brute-force | ✅ 重要修复 |

**无新 bug 或技术债务。** 新修复质量良好，OIDC 合规性显著提升。

---

## 5. 问题汇总

### 本次新发现

**无新问题。** 本次审查确认了 5 个新修复（c_hash/at_hash/id_token refresh/scope enforcement/brute-force），所有修复正确。

### 之前报告活跃问题

| ID | 严重性 | 状态 | 描述 |
|----|--------|------|------|
| P1-2 | P1 | 活跃 | Go SDK iss 验证 |
| P1-3 | P1 | 活跃 | MCP JWKS 实现 |
| P1-6 | P1 | 活跃 | JWKS rotation 旧 key |
| P2-31~P2-57 | P2 | 活跃 | 50 个 P2（详见 R35/R36）|

### 已修复确认（累计）

P1-1 ✅, P1-4 ✅, P1-5 ✅, P1-7 ✅, P1-9 ✅, P1-10 ✅, P2-9 ✅, P2-56 ✅
新增: c_hash/at_hash ✅, id_token refresh ✅, scope enforcement ✅, brute-force ✅, SQL injection ✅

**总计：53 个活跃问题（P0: 0 / P1: 3 / P2: 50）**