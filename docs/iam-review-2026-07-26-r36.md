# IAM 系统深度审查报告 — 2026-07-26 (R36)

## 审查范围

全新视角独立审视。聚焦之前报告未深入覆盖的领域：OIDC id_token 验证链、SAML IdP 密钥安全、RLS 配置、gRPC 通道认证、密钥旋转原子性、P1 修复验证、新 commit 审查。

---

## 1. RFC 标准符合性

之前 R33-R35 报告已详细覆盖。本次确认无新差距。

## 2. 竞品迁移成本

之前报告已覆盖。无新发现。

## 3. 安全审查（新维度）

### 3.1 OIDC id_token 验证链

| 检查项 | 状态 | 说明 |
|--------|------|------|
| id_token 签发 | ✅ | issueIDToken (oauth_service.go:1128) — RS256 + nonce + iss/sub/aud |
| id_token 验证 | ❌ | 无 VerifyIDToken/ParseIDToken 函数 — id_token 仅签发不验证（资源服务器验证 access_token，不验证 id_token）|

### 3.2 SAML IdP 密钥安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 密钥来源 | ✅ | 优先 SAML_IDP_KEY_PATH → DB sys_config → ephemeral |
| Ephemeral 警告 | ✅ | server.go:2743 有 slog.Warn |
| Ephemeral 风险 | ⚠️ | 重启后 SP trust 断裂，生产应配置持久密钥 |

### 3.3 RLS 配置

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 参数化设置 | ✅ | `SET app.tenant_id = $1` 使用参数化查询 |
| Gateway 注入 | ✅ | X-Tenant-ID 由 gateway 从 JWT claim 验证后注入 |
| 超级用户绕过 | ⚠️ | Docker 默认超级用户绕过 RLS — 生产需非超级用户角色（已知）|

### 3.4 gRPC 通道认证

| 检查项 | 状态 | 说明 |
|--------|------|------|
| gRPC TLS | ⚠️ | org/policy 服务可选 TLS (GRPC_TLS_ENABLED)，未启用时 plaintext |
| gRPC 认证 | ❌ | 无 gRPC 通道级认证 — 依赖网络隔离 |

### 3.5 密钥旋转原子性

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Mutex 保护 | ✅ | RotateKey 使用 r.mu.Lock() |
| 原子性 | ✅ | previous=current → current=newKey 在同一锁内完成 |
| JWKS 一致性 | ❌ | GetJWKS 不暴露旧 key (P1-6 已记录) |

---

## 4. 代码质量 — 最近 20 commits

| Commit | 类型 | 问题 |
|--------|------|------|
| 05121b636 docs | — | 无 |
| e1277960a docs | — | 无 |
| 1ed1d931e test | — | auth code flow E2E 测试 — 好 |
| 3a56c3c78 fix | P1-4/P1-5 | PII 脱敏 — 正确 |
| 5b6585510 docs | — | 无 |
| 5296599d0 fix | P1-7 | post_logout_redirect_uri 验证 — 正确 |
| 5d14b6638 docs | — | 无 |
| c6d65ab9b fix | P1-1 | refresh token client binding + logout.go fix — 正确 |
| 62172ce39 docs | — | 无 |
| 919e2b2a8 docs | — | 记录 Redis fallback client_id gap |
| 933d2489c docs | — | 无 |
| 3ff1eaf05 fix | audit | webhook handler DB persistence — 正确 |
| 47f6c2df7 fix | P2-9 | PKCE plain 移除 — 正确 |
| 其余 | docs | 无 |

**无新 bug 或技术债务。** 修复质量良好。

---

## 5. P1 修复验证

| ID | 修复 commit | 状态 | 验证 |
|----|------------|------|------|
| P1-1 | c6d65ab9b | ✅ 主路径修复 | oauth_service.go:1643-1647 `record.ClientID != client.ID` |
| P1-1 Redis fallback | — | ⚠️ 已知缺口 | lookupAuthRefreshToken 无 client_id 检查（有注释说明）|
| P1-4 | 3a56c3c78 | ✅ | maskEmail() 脱敏 |
| P1-5 | 3a56c3c78 | ✅ | slog + maskUser() |
| P1-7 | 5296599d0 | ✅ | logout.go:77 验证注册 URI |
| P1-10 | — | ❌ 未修复 | CreateRole 仍不验证 name |

---

## 6. 问题汇总

### 本次新发现

| ID | 严重性 | 描述 |
|----|--------|------|
| P2-53 | P2 | OIDC id_token 无验证端 — 仅签发不验证，资源服务器无法验证 id_token（仅验证 access_token）|
| P2-54 | P2 | gRPC 通道无认证 — 依赖网络隔离，未启用时 plaintext |
| P2-55 | P2 | SAML IdP ephemeral key 风险 — 重启后 SP trust 断裂（有 warning 但无强制阻止）|
| P2-56 | P2 | P1-1 Redis fallback 无 client_id 绑定 — Auth-issued refresh token 可被任意 client 刷新（主路径已修复）|

### 之前报告活跃问题确认

- P1-2 Go SDK iss ❌ 活跃
- P1-3 MCP JWKS ❌ 活跃
- P1-6 JWKS rotation ❌ 活跃
- P1-7 post_logout_redirect_uri ✅ **已修复** (5296599d0)
- P1-9 TOTP plaintext ❌ 活跃
- P1-10 role name impersonation ❌ **未修复**
- P2-31~P2-52 ❌ 活跃（48 个）

### 已修复（本次确认）
- P1-7 ✅ post_logout_redirect_uri 验证注册 URI (logout.go:77)

**总计：56 个活跃问题（P0: 0 / P1: 5 / P2: 51）**